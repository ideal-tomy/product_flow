/**
 * CORE-CANDIDATE — reuse entry for product_flow Phase 3+.
 * Minimal vendor set: usage normalizer only. Full sendAiRequest lands in Phase 3 full.
 */
export { normalizeUsage } from "./usage-normalizer";
