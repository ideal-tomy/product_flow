/**
 * Brand config (minimal) — storage prefix only; no full theme swap.
 */
export const brandConfig = {
  id: "axeon",
  companyName: "AXEON",
  productName: "ConformSystem Demo",
  storageNamespace: "axeon",
};

export type BrandConfig = typeof brandConfig;
