# ConformSystem AI Mode（任意）
# 設定すると POST /api/ask が OpenAI 構造化回答を試行します。
# 未設定時はナレッジパックの RAG シンセサイザのみで動作します。
#
# ローカル: .env.local に記載（npm run dev）
# Vercel: Project Settings → Environment Variables に同名で登録
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-nano
